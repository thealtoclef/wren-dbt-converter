jaffle_shop:

  target: dev
  outputs:
    dev:
      type: sqlite
      database: database
      schema: main
      schemas_and_paths:
        main: main.db
      schema_directory: .
      threads: 24
